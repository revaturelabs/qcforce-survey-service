package com.revature.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class QuestionController {
	/*
	 * private QuestionService questionService;
	 * 
	 * @Autowired public void setquestionService(QuestionService questionService) {
	 * this.questionService = questionService; }
	 * 
	 * @GetMapping("/question") public List<Question> getAllForms() { return
	 * questionService.getAllQuestions(); }
	 * 
	 * @GetMapping("/question/{formId}") public List<Question>
	 * getQuestionByFormId(@PathVariable("formId")int formId) { return
	 * (List<Question>) questionService.getQuestionByFormId(formId); }
	 * 
	 * @GetMapping("/question/{questionType}") public List<Question>
	 * getQuestionByQuestionType(@PathVariable("questionType")String questionType) {
	 * return (List<Question>)
	 * questionService.getQuestionByQuestionType(questionType); }
	 * 
	 * @PostMapping("/question") public String createQuestion(@RequestBody Question
	 * question) { questionService.createQuestion(question); return
	 * "Question successfully created"; }
	 * 
	 * @PutMapping("/question") public String updateQuestion(@RequestBody Question
	 * question) { questionService.updateQuestion(question); return
	 * "Question successfully updated"; }
	 * 
	 * @DeleteMapping("/Question/{questionId}") public String
	 * deleteQuestion(@PathParam("questionId") int questionId) { Question question =
	 * new Question(); question.setQuestionId(questionId);
	 * questionService.deleteQuestion(question); return "Form successfully deleted";
	 * }
	 */

}

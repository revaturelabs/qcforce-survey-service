package com.revature.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class NotificationController {

	/*
	 * private NotificationService notificationService;
	 * 
	 * 
	 * @Autowired public void setNotificationService(NotificationService
	 * notificationService) { this.notificationService = notificationService; }
	 * 
	 * @GetMapping("/notification") public List<Notification> getAllForms() { return
	 * notificationService.getAllNotification(); }
	 * 
	 * @GetMapping("/notification/{batchName}") public List<Notification>
	 * getNotificationByBatchName(@PathVariable("batchName")String batchName) {
	 * return notificationService.getNotificationByBatchName(batchName); }
	 * 
	 * @PostMapping("/notification") public String createNotification(@RequestBody
	 * Notification notification) {
	 * notificationService.createNotification(notification); return
	 * "Notification successfully created"; }
	 * 
	 * @PutMapping("/notification") public String updateForm(@RequestBody
	 * Notification notification) {
	 * notificationService.updateNotification(notification); return
	 * "Notification successfully updated"; }
	 * 
	 * @DeleteMapping("/notification/{notificationId}") public String
	 * deleteNotification(@PathParam("notificationId") int notificationId) {
	 * Notification notification = new Notification();
	 * notification.setNotificationId(notificationId);
	 * notificationService.deleteNotification(notification); return
	 * "Form successfully deleted"; }
	 */

}
